import { type Express } from 'express';
/**
 ** Sets up the webhooks for the application.
 * @param app - The Express application instance.
 */
declare const setupWebhooks: (app: Express) => void;
export default setupWebhooks;
